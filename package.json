{
  "name": "react-allowed",
  "version": "0.0.0",
  "description": "a react component that calls a function prop isAllowed to determine whether or not to render children",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "react",
    "permissions",
    "branch"
  ],
  "author": "Conor Hastings",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  }
}
